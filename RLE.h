#ifndef RLE_H
#define RLE_H
#include <cmath>
#include <vector>
using namespace std;

vector <unsigned char> RLE (vector<unsigned char> &bits);
vector <unsigned char> RLE (vector<unsigned char> &bits);
int binToInt(vector<unsigned char> &bin);

#endif