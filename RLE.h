#ifndef RLE_H
#define RLE_H
#include <cmath>
#include <vector>
#include "bitUtil.h"
using namespace std;

vector <unsigned char> RLE (vector<unsigned char> &bits);
vector <unsigned char> RLE (vector<unsigned char> &bits);

#endif