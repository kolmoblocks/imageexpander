#include "lodepng.h"
#include <iostream>
#include <fstream>
#include <cmath>
#include <unordered_set> 
#include <numeric> 
#include <string>

void writeMBlock(std::vector<MB) {
    
    char id = 'M';
    fwrite()
    while (pos != end) {
        
        ++pos;
    }
}